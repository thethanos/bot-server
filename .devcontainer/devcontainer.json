{
    "name" : "bot-dev",
    "build" : {
        "context" : "..",
        "dockerfile" : "../docker/dev.Dockerfile"
    },

    "customizations": {
        "vscode": {
            "extensions": [
                "golang.go",
                "golang.go-nightly"
            ]
        }
    },

    "runArgs": [
        "--network=home_default"
    ]
}